1. Написать базовый класс (или интерфейс) (элемент директории)
Для удобства: метод AsFile и AsDirectory, для удобной приводимости типов (смотреть на Type)
Свойство abstract Type { get; } (конечно enum).
поле/свойство ссылка на директорию, в которой он находится (null если это корень).

2. Написать два наследника (файл и директория)

3. У файла будет два поля название файла и текст. И метод, возращающий содержимое файла (virtual).

4. У директории будет два поля - название директории и список содержимого директории (список объектов класса, описанных в первом пункте).
метод List<GetContent> GetConent.

5. Грубо говоря в итоге получится дерево. 

6. Написать класс FileSystem, который хранит указатель на корень файловой системы. Написать методы добавления директории/файл, удаление директории/файла, обход дерева файловой системы (гугл).